package AtCoderBeginnerContest042

import java.time.temporal.TemporalAmount

class IrohasObsession {
  def main(args: Array[String]): Unit = {
    // val input = readLine
//    val List(amount: Int, countOfdislikeWord: Int) = new java.util.Scanner(System.in).toString.split(" ").toList.map(_.toInt)
//    val listOfDislikeWord = new java.util.Scanner(System.in).toString.split(" ").toList.map(_.toInt)
//    // val likeNumberList = makeLikeNumberList(listOfDislikeWord)
//    // likeNumberListに格納されている数字から、合計金額と同じ桁数の最小の数字を作る
//    val answer = (amount to (10*amount)).find(p => p >= amount && !isContainsLikeNumber(listOfDislikeWord, amount))
//    println(answer.get)
//
//    def isContainsLikeNumber(listOfDislikeWord: List[Int], amount: Int): Boolean = {
//      // 合計金額を桁ごとにListにしたもの
//      val amountList = amount.toString.toList.map(_.asDigit)
//      amountList.exists(p => listOfDislikeWord.contains(p))
//    }

//    def makeLikeNumberList(listOfDislikeWord: List[Int]): List[Int] = {
//      (1 to 9).toList.filterNot(num => listOfDislikeWord.contains(num))
//    }
  }
}
